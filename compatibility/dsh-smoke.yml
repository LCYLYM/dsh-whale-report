schema: 1
requires_model_turn: false
web:
  ready:
    path: /
    timeout_seconds: 120
  assertions:
    - id: whale-report-list-api
      method: GET
      path: /whale/api/list
      status: 200
      json_subset:
        ok: true
